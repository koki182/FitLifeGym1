const form=document.getElementById('enquiryForm');const message=document.getElementById('formMessage');if(form){const params=new URLSearchParams(window.location.search);const selected=params.get('service');if(selected)document.getElementById('service').value=selected;form.addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('fullName').value.trim();message.textContent='Thank you, '+name+'. Your enquiry has been submitted successfully. We will contact you soon.';form.reset();if(selected)document.getElementById('service').value=selected;});}
